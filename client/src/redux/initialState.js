const initialState = {
    auth: {
        logged: false,
        loginRedirect: false,
        logoutRedirect: false,
    },
    user: {
        list: {},
        loginRedirect: false
    }
};

export default initialState