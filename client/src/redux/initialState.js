const initialState = {
    auth: {
        logged: false,
        loginRedirect: false,
        logoutRedirect: false,
        newUser: null
    },
    users: {
        list: {}
    }
};

export default initialState